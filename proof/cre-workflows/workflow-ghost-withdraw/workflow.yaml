# ==========================================================================
# Workflow: Ghost Withdraw (v2 — USDC-direct)
# ==========================================================================
# Validates DON state, checks claims, verifies backing, publishes attestation.
# Run: cre workflow simulate workflow-ghost-withdraw --target local-simulation

local-simulation:
  user-workflow:
    workflow-name: "workflow-ghost-withdraw"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.json"
    secrets-path: "./secrets.yaml"

staging:
  user-workflow:
    workflow-name: "workflow-ghost-withdraw"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.json"
    secrets-path: "./secrets.yaml"
