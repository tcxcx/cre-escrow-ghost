# Secrets for workflow-escrow-deploy only
secretsNames:
  SUPABASE_SERVICE_KEY:
    - SUPABASE_SERVICE_KEY_VAR
  SUPABASE_URL:
    - SUPABASE_URL_VAR
  CRE_ETH_PRIVATE_KEY:
    - CRE_ETH_PRIVATE_KEY_VAR
