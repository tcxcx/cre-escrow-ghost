# Secrets for workflow-escrow-verify only
secretsNames:
  SUPABASE_SERVICE_KEY:
    - SUPABASE_SERVICE_KEY_VAR
  SUPABASE_URL:
    - SUPABASE_URL_VAR
  SHIVA_API_URL:
    - SHIVA_API_URL_VAR
  SHIVA_SERVICE_TOKEN:
    - SHIVA_SERVICE_TOKEN_VAR
  CRE_ETH_PRIVATE_KEY:
    - CRE_ETH_PRIVATE_KEY_VAR
