# Secrets managed via: cre secrets create -R ..
# Required secrets for BUFI lifecycle workflows:
#   OPENAI_API_KEY      — Layer 1 verifier, Layer 2 advocates
#   ANTHROPIC_API_KEY   — Layer 1 verifier, Layer 2 advocates, jurors
#   GEMINI_API_KEY      — Layer 3/4 juror (Google)
#   SUPABASE_URL        — Artifact storage
#   SUPABASE_KEY        — Artifact storage (service role key)
